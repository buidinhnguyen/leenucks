#!/bin/zsh
# vim:fenc=utf-8:nu:ai:si:et:ts=2:sw=2:

echo "The databases in '${HOME}/.mozilla' are being optimized..."
for i in $(find ~/.mozilla -name \*.sqlite); sqlite3 $i vacuum
for i in $(find ~/.mozilla -name \*.sqlite); sqlite3 $i reindex
echo "Done."

