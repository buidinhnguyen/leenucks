diff -aurp dwm-5.9.old/dwm.c dwm-5.9/dwm.c
--- dwm-5.9.old/dwm.c	2011-07-10 22:24:25.000000000 +0200
+++ dwm-5.9/dwm.c	2011-10-07 13:45:03.869066997 +0200
@@ -1113,8 +1113,14 @@ manage(Window w, XWindowAttributes *wa)
 		applyrules(c);
 	}
 	/* geometry */
-	c->x = c->oldx = wa->x;
-	c->y = c->oldy = wa->y;
+	if(!c->mon->lt[c->mon->sellt]->arrange || c->isfloating) {
+		c->x = c->oldx = (c->mon->mw - wa->width) / 2;
+		c->y = c->oldy = (c->mon->mh - wa->height) / 2;
+	}
+	else {
+		c->x = c->oldx = wa->x;
+		c->y = c->oldy = wa->y;
+	}
 	c->w = c->oldw = wa->width;
 	c->h = c->oldh = wa->height;
 	c->oldbw = wa->border_width;
